{% extends 'base.html.twig' %}

{% block title %}Introduction Naturaliste{% endblock %}

{% block body %}
    {% include("inc/brainAnimation.html.twig")%}
    <div class="ui grid container h100 middle aligned" id="pageIntro">
        <div class="row">
            <div class="sixteen wide mobile ten wide tablet eight wide computer centered column">
                <div class="ui colorApp segment">
                    <h3 class="ui dividing header" >Introduction au jeu qui va suivre :</h3>
                    <h3 class="ui header centered" id="introTitleNaturaliste">Intelligence Naturaliste</h3>
                    <div class="explanations">
                        <p>A chaque manche, tu devras trouver, parmi les 4 images qui te seront présentées, celle qui ne
                            présente pas de point commun avec les 3 autres.</p>
                        <p>Bonne chance !</p>
                    </div>
                    {# modal #}
                    <div class="ui mini modal demo" id="modalGifNaturaliste">
                        <h4 class="ui block header naturaliste centered">Aperçu du jeu suivant</h4>
                        <img src="{{ asset('gifs/gifNaturaliste.gif') }}" alt="gif demonstration" width="100%">
                    </div>
                    {# modal #}
                    <div class="introButtons">
                        <a class="ui button" href="{{ path('naturaliste') }}" onclick="playClick()">Je
                            suis prêt !</a>
                        <i class="question circle outline icon" id="demoGifNaturaliste"
                           onclick="afficherModalDemo(); playLink()" title="Aperçu du jeu suivant"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
