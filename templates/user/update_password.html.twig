{% extends 'base_admin.html.twig' %}

{% block title %}Modifier votre mot de passe{% endblock %}

{% block body %}
    <div class="ui grid container padded">
        <div class="row">
            <div class="sixteen wide mobile twelve wide tablet eight wide computer centered column">
                <div class="ui segment">
                    <h3 class="ui dividing header">Modifier votre mot de passe</h3>
                    {{ form_start(changePasswordFormType) }}
                    <div class="field">
                        {{ form_row(changePasswordFormType.plainPassword) }}
                    </div>
                    <div class="field">
                        {{ form_row(changePasswordFormType.save) }}
                    </div>
                    {{ form_end(changePasswordFormType) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
{% endblock %}
